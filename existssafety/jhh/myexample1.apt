// with fl(t) = {(E1,E2)} -> ENV wins
// with fl(t) = {} -> SYS wins
.name "unfoldingAdditionalSystem"
.type LPN
.description "Testing the correctness of additional system places in the unfolding"
.options
winningCondition="SAFETY"

.places
E1[env="true"]
E2[bad="true",env="true"]

.transitions
t

.flows
t: {E1} -> {E2}

.initial_marking {E1}
