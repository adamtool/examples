// forced empty fl(t) -> SYS wins because initial token is consumed without ever visiting bad place
.name "unfoldingAdditionalSystem"
.type LPN
.description "Testing the correctness of additional system places in the unfolding"
.options
winningCondition="SAFETY"

.places
E1[env="true"]
E2[bad="true",env="true"]

.transitions
t[tfl="{}"]

.flows
t: {E1} -> {E2}

.initial_marking {E1}
